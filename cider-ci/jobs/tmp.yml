jobs:

  tmp:

    name: TMP

    description: |
      Run TMP scenarios

    run-on:
    - type: branch
      include-match: ^.*$

    context:
      task-defaults:
        max-auto-trials: 1

      _cider-ci_include:
        - cider-ci/shared/tests-context.yml
        - cider-ci/tasks/tmp.yml
