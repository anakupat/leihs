script_defaults:
  template_environment_variables: true

task_defaults:

  include: cider-ci_v4/task_components/bundle_rspec_ruby.yml

  environment_variables:
    RUBY_ENGINE: ruby
    RUBY_VERSION: 2.3.0
    RUBY: '{{RUBY_ENGINE}}-{{RUBY_VERSION}}'
    RAILS_ENV: test

  max_trials: 3
